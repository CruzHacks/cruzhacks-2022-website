export interface social {
    link: string
    Icon: any
    label: string
  }

export interface theme {
  bgColor: string
  iconFill: string
  iconStroke: string
  textColor: string
  hillColor: string

}
export interface FooterProps {
    theme : theme
  }
